

HOW TO IMPORT A FILE WITH MULTIPLE DELIMITERS

my_cols = [str(i) for i in range(45)] # create some col names
df_user_key_word_org = pd.read_csv(filepath+"user_key_word.txt",
                                   sep="\s+|;|:",
                                   names=my_cols, 
                                   header=None, 
                                   engine="python")
# I tested with s = StringIO(text_from_OP) on my computer

Basically you let pandas know how many columns to accept, in advance


HOW TO SPLIT A STRING BY MULTIPLE DELIMITER





